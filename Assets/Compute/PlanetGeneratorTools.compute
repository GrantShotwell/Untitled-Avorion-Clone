

int2 resolution;
RWStructuredBuffer<float3> vertices;

int GetIndex(uint3 id) {
	// Flattens a 2D array.
	uint2 usize = uint2((uint)resolution.x, (uint)resolution.y);
	uint index = (id.x) + (id.y * usize.x);
	return (int)index;
}
